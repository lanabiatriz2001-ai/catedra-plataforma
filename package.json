{
  "name": "catedra-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Deploy da plataforma Cátedra na Vercel com IA (Claude) via função serverless.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node tests/run.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.49.0"
  }
}
